import 'package:flutter/material.dart';

// This class keeps all project colours in one place.
// The palette uses dark navy and soft green.
class AppColors {
  static const bgDark = Color(0xFF0B1F2A);
  static const panelDark = Color(0xFF102D3A);
  static const panelLight = Color(0xFF153847);
  static const sidebarDark = Color(0xFF173746);
  static const accentGreen = Color(0xFF00D084);
  static const textWhite = Color(0xFFF5F7FA);
  static const textMuted = Color(0xFF9FB3C8);
  static const borderDark = Color(0xFF244B5A);
  static const danger = Color(0xFFFF6B6B);
  static const warning = Color(0xFFFFC857);
}
